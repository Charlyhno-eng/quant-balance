import { Box, Typography } from '@mui/material';

export default function PortfolioBalancer() {
  return (
    <Box sx={{ bgcolor: 'rgba(10, 26, 51, 0.7)', borderRadius: 1, height: '100%' }}>
      <Typography sx={{ p: 2, color: 'white' }}>Graphique Deux</Typography>
    </Box>
  );
}
